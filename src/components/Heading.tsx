
export const Heading = ({headingText}) => {

  return (
    <div className="container flex justify-center">
        <h1 className={`font-extrabold p-4 text-[30px] text-amber-100`}>{headingText}</h1>
    </div>
  )
}
